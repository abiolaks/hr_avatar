# =============================================================================
# INSTALL INSTRUCTIONS
# Step 1:  pip install -r requirements.txt
# Step 2:  pip install TTS==0.22.0 --no-deps   (bypasses its broken numpy pin)
# Step 3:  git clone https://github.com/justinjohn0306/Wav2Lip.git face/wav2lip
# =============================================================================

# Core
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
numpy==1.24.3
pydub==0.25.1
librosa==0.10.2
soundfile==0.12.1

# Brain & Tools
langchain==0.2.0
langchain-community==0.2.0
langgraph==0.1.19          # 0.0.x conflicts with langchain-core 0.2.x
chromadb==0.4.22           # 0.5.x requires numpy>=1.22.5 which conflicts with TTS
ollama>=0.3.0
langchain-ollama==0.1.3
requests==2.31.0

# VAD
silero-vad==5.1
pyaudio==0.2.14

# Transcription
faster-whisper==1.0.3      # 0.9.x pulls av==10 which fails to compile on newer Cython

# Voice
# TTS==0.22.0 conflicts: pins numpy==1.22.0 exactly on Python<=3.10
# Install after this file: pip install TTS==0.22.0 --no-deps

# Face
opencv-python==4.9.0.80
facenet-pytorch==2.6.0
# wav2lip is not a pip package — clone manually:
#   git clone https://github.com/justinjohn0306/Wav2Lip.git face/wav2lip
gdown==5.1.0
pypdf                      # needed to load PDF files into RAG
docx2txt                   # needed to load .docx files into RAG
azure-storage-blob         # Azure Blob Storage — company document store integration

# Web (optional)
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# TTS runtime dependencies (installed separately due to numpy pin conflict)
# Run: pip install TTS==0.22.0 --no-deps  then install these:
coqpit
trainer
transformers==4.44.2        # BeamSearchScorer removed in 4.47+, TTS 0.22.0 needs it
einops
encodec
unidecode
inflect
num2words
pysbd
anyascii
pandas>=1.4,<2.0
matplotlib
bangla
bnnumerizer
bnunicodenormalizer
hangul_romanize
jamo
jieba
nltk
pypinyin
gruut[de,es,fr]==2.2.3
umap-learn
cython
flask
spacy                      # needed by XTTS tokenizer at import time

# Testing
pytest==8.1.1
pytest-mock==3.12.0
